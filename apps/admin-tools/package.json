{
  "name": "@festival/admin-tools",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/seed-festival.js",
    "test": "node -e \"console.log('No admin-tools tests configured')\"",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.3"
  }
}
